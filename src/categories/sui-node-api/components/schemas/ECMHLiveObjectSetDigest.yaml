description: "The Sha256 digest of an EllipticCurveMultisetHash committing to the live object set."
type: object
required:
  - digest
properties:
  digest:
    type: array
    items:
      type: integer
      format: uint8
      minimum: 0
    maxItems: 32
    minItems: 32
