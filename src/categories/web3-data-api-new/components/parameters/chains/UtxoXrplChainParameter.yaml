name: chain
in: path
required: true
schema:
  type: string
  description: |
    조회할 체인을 지정하는 파라미터입니다. API에 따라 지원되는 체인이 다를 수 있습니다.
  enum:
    - bitcoin
    - dogecoin
    - bitcoincash
    - xrpl
  default: bitcoin
