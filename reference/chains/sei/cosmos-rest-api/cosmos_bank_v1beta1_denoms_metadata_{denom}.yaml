openapi: 3.1.0
info:
  title: SEI - cosmos_bank_v1beta1_denoms_metadata_{denom}
  version: 1.0.0
servers:
  - url: https://rest-sei-mainnet.nodit.io
components:
  securitySchemes:
    api_key:
      type: apiKey
      name: X-API-KEY
      in: header
      x-default: nodit-demo
      description: |
        The example value `nodit-demo` is for use in the developer documentation only.
        For real applications or services, please use the API key issued from the Nodit console.
paths:
  /cosmos/bank/v1beta1/denoms_metadata/{denom}:
    get:
      description: DenomMetadata queries the client metadata of a given coin denomination.
      operationId: cosmos_bank_v1beta1_denoms_metadata_{denom}
      tags:
        - bank
      responses:
        "200":
          description: Successful Response
          content:
            "*/*":
              schema:
                type: object
                properties:
                  metadata:
                    type: object
                    properties:
                      description:
                        type: string
                      denom_units:
                        type: array
                        items:
                          type: object
                          properties:
                            denom:
                              type: string
                              description: denom represents the string name of the given denom unit (e.g uatom).
                            exponent:
                              type: integer
                              format: int64
                              description: |-
                                exponent represents power of 10 exponent that one must
                                raise the base_denom to in order to equal the given DenomUnit's denom
                                1 denom = 10^exponent base_denom
                                (e.g. with a base_denom of uatom, one can create a DenomUnit of 'atom' with
                                exponent = 6, thus: 1 atom = 10^6 uatom).
                            aliases:
                              type: array
                              items:
                                type: string
                              title: aliases is a list of string aliases for the given denom
                          description: |-
                            DenomUnit represents a struct that describes a given
                            denomination unit of the basic token.
                        title: denom_units represents the list of DenomUnit's for a given coin
                      base:
                        type: string
                        description: base represents the base denom (should be the DenomUnit with exponent = 0).
                      display:
                        type: string
                        description: |-
                          display indicates the suggested denom that should be
                          displayed in clients.
                      name:
                        type: string
                        title: "name defines the name of the token (eg: Cosmos Atom)"
                      symbol:
                        type: string
                        description: Token symbol primarily displayed on exchanges (e.g., ATOM). May be the same as display.
                      uri:
                        type: string
                        description: URI of a document (on-chain or off-chain) containing additional information. Optional.
                      uri_hash:
                        type: string
                        description: SHA256 hash of the document the URI points to. Used to verify document integrity. Optional.
                    description: |-
                      Metadata represents a struct that describes
                      a basic token.
                description: |-
                  QueryDenomMetadataResponse is the response type for the Query/DenomMetadata RPC
                  method.
        default:
          description: An unexpected error response.
          content:
            "*/*":
              schema:
                type: object
                properties:
                  error:
                    type: string
                  code:
                    type: integer
                    format: int32
                  message:
                    type: string
                  details:
                    type: array
                    items:
                      type: object
                      properties:
                        type_url:
                          type: string
                        value:
                          type: string
                          format: byte
      parameters:
        - name: denom
          description: denom is the coin denom to query the metadata for.
          in: path
          required: true
          schema:
            type: string
      security:
        - api_key: []
      summary: /cosmos/bank/v1beta1/denoms_metadata/{denom}
